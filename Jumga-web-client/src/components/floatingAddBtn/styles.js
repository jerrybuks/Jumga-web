import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    addBtn: {
       position: "fixed",
       bottom: 45,
       zIndex: 333
      //  right: 580
    }
}));


